// Immutable v0.5.1 definitions live here (populated in "Extract types and
// conversion registry"). The scaffold pins only the schema-version constant
// so the public surface has a stable anchor from the first commit.

/** The frozen export/import contract version (schemaVersion 7). */
export const SCHEMA_VERSION = 7;
